# Lyte source map

Observed 2026-09-04 15:40 UTC. `szl-holdings/lyte-services@def1133…`
is selected as the dedicated product source with status
`SELECTED_WITH_CONFLICT`. The protected `szl-holdings/a11oy@07c2d3f…` workflow
is the observed Hugging Face single writer. The older live product-source policy
still points at `lyte-lattice`; that conflict must be superseded on protected
main before deployment authority is claimed.

`vertical-services` and `lyte-lattice` remain preserved references. They are not
parallel canonical implementations. Formula, receipt, anatomy, and retrieval
repositories contribute only the explicitly recorded patterns in
`reuse-decisions.json`.
